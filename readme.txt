Steps to create masks from WSIs

1. Clone repo.
- git clone https://github.com/hwanglab/tcga_stroma_prediction.git

2. Move to "tcga_stroma_prediction" directory

3. Run python
- python generate_mask_from_WSIs.py with arguments

**1) you may need to install some python packages.
**2) you may need to change input/output directory (test just one slide first).

4. Confirm results in 'your output directory'
